#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../simpleunit/bin/simpleunit" "$@"
  ret=$?
else 
  node  "$basedir/../simpleunit/bin/simpleunit" "$@"
  ret=$?
fi
exit $ret
